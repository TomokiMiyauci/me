import { line } from '@/firestore/line'
import { fcm } from '@/firestore/fcm'
import { meta } from '@/firestore/meta'
import { users } from '@/firestore/users'

export const firestore = {
  line,
  fcm,
  meta,
  users
}
